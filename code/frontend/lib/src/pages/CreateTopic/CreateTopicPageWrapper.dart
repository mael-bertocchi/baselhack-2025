import 'package:flutter/material.dart';
import '../../Application/CreateTopic/UI/CreateTopicPage.dart';

/// Page wrapper for Create Topic page
/// This maintains consistency with the application's page structure
class CreateTopicPageWrapper extends StatelessWidget {
  const CreateTopicPageWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const CreateTopicPage();
  }
}
