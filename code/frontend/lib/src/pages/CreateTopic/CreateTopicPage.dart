import 'package:flutter/material.dart';
import '../../Application/CreateTopic/UI/CreateTopicView.dart';

/// Page wrapper for Create Topic page
/// This maintains consistency with the application's page structure
class CreateTopicPage extends StatelessWidget {
  const CreateTopicPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const CreateTopicView();
  }
}
